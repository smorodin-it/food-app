import { frontendDataAccessAuth } from './frontend-data-access-auth';

describe('frontendDataAccessAuth', () => {
  it('should work', () => {
    expect(frontendDataAccessAuth()).toEqual('frontend-data-access-auth');
  });
});
